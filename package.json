{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "./node_modules/sass/sass.js css/style.scss css/style.css",
    "clean-css":	"./node_modules/clean-css-cli/bin/cleancss -o dist/style.min.css css/style.css",
    "build":	"mkdir -p dist && npm run sass && npm run clean-css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "clean-css-cli": "^4.3.0",
    "sass": "^1.18.0"
  }
}
